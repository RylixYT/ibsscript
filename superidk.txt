$serialNumber = Get-WmiObject -Class Win32_BIOS | Select-Object -ExpandProperty SerialNumber
$pcName = $env:COMPUTERNAME

$data = @"
Seriennummer,PC-Name
$serialNumber,$pcName
"@

$csvPath = "data_$pcName.csv"
$data | Out-File -FilePath $csvPath -Encoding UTF8

Invoke-Expression $csvPath