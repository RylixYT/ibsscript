$computerName = $env:COMPUTERNAME

$lastUser = Get-WmiObject -Class Win32_ComputerSystem |
            Select-Object -ExpandProperty UserName

if ($lastUser) {
    $lastUser = $lastUser.Split('\')[-1]
    Write-Output "Der letzte angemeldete Benutzer auf $computerName war: $lastUser"
} else {
    Write-Output "Es wurde kein Benutzer auf $computerName gefunden."
}